package com.math;

import com.byteArray.intToByteArray;

public class Expression {

	public static void main(String[] args) {
		double x=0,y=0,a=0,r=0,s,R=0;
		s = (4/3)*Math.PI*Math.pow(r, 3);
		System.out.println(".."+Math.pow(3, 3));
		s = 1/(1/r+1/s);
		s = Math.pow(r, 5)+Math.pow(r, 3)+6;
		s = r*s/(R*R);
		System.out.println(".."+Math.abs(-369.3));
		System.out.println(".."+Math.abs(69.3));
		s = Math.sin(x)/(a*x)+Math.abs(Math.cos(Math.PI*x/2));
		if(a>0&&a<10);
		if((x==1)!=(y==2));
	}
}
